// import test from "node:test";

// import { getLatestSemverInMajor } from "./getLatestSemverInMajor";

// test("gets latest 0.x.x tag correctly", async () => {
//   const tags = ["v0.6.7", "v1.2.4", "v0.0.9", "v0.5.1"];
//   const result = await getLatestSemverInMajor(tags, 0);
//   expect(result).toBe("0.6.7");
// });
